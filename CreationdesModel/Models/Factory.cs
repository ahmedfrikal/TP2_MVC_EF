using Microsoft.EntityFrameworkCore;

namespace CreationdesModel.Models
{
    public class Factory
    {
       /* public static Mycontext getMycontext()
        {
            DbContextOptionsBuilder<Mycontext> opt = new DbContextOptionsBuilder<Mycontext>();
            SqlServerDbContextOptionsExtensions.UseSqlServer(opt);
            opt.UseSqlServer("Data Source=DESKTOP-3TG5K6V\\SQLEXPRESS;Initial Catalog=creationbd;Integrated Security=True");
            return new Mycontext(opt.Options);
        }*/
    }
}
